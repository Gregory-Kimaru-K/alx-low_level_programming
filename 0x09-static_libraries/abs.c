#include "main.h"
/**
 * _abs - prints out the absolute number
 *@n: the character it checks
 * Return: return an absolute number
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
