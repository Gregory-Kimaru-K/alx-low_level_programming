#include "main.h"
#include <stdlib.h>
/**
 * malloc_checked - allocates memory
 * @b: memory size
 * Return: returns void
 */
void *malloc_checked(unsigned int b)
{
	int *_alocate = malloc(b);

	if (alocate == NULL)
		exit(98);

	return (alocate);
}
